# templates/env/evolution-api.env.example
# Copiado/gerado automaticamente por modules/evolution-api/install.sh — não
# editar o .env real deste módulo manualmente sem necessidade.

DOMAIN=whatsapp.seudominio.com.br
POSTGRES_PASSWORD=troque-por-senha-gerada-no-install
API_KEY=troque-por-chave-gerada-no-install
TZ=America/Sao_Paulo
